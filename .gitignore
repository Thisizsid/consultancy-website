# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local environment variables — .env.production is NOT listed here:
# it holds only the public VITE_API_URL (no secrets) and is required for
# `npm run build` to produce a working production bundle on a fresh clone.
.env
.env.local
.env.*.local
.env.development
server/.env
server/.env.local

# Tooling outputs and cache
.claude/
graphify-out/

# Build / deployment artifacts
dist.zip
frontend_update.zip
server_upload.zip
*.zip

# Server (backend) — track source, exclude secrets, data and artifacts
server/node_modules/
server/database.sqlite
server/uploads/

# SQLite data files anywhere in the repo, plus WAL-mode sidecar files
*.sqlite
*.sqlite3
*.db
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Vercel CLI local project link (contains org/project IDs)
.vercel

# Generated server config
public/.htaccess
.env.bak*
NEW_ADMIN_PASSWORD.txt

# SEO audit report (local scratch, not for repo)
SEO-AUDIT-REPORT.html
